# Video BA Pipe 页面设计规范

> 本文是前端页面设计与实现的唯一基线，适用于新增页面、组件重构和视觉评审。
> 当前规范由现有页面、`frontend/src/global.css`、`frontend/src/theme/index.ts` 与公共组件提炼而来。
> 最后更新：2026-09-10。

## 1. 产品与设计定位

### 1.1 产品语境

Video BA Pipe 是面向视频分析运维人员、算法工程师和系统管理员的实时控制台。界面的核心任务不是展示品牌，而是让用户快速判断：

- 视频与推理链路是否正常；
- 当前有哪些资产、任务和事件；
- 风险发生在哪里，下一步应采取什么操作；
- 配置会影响什么对象，操作是否已经生效。

### 1.2 设计性格

整体风格定义为：**克制、精密、可信的实时视频运营工作台**。

- 克制：大面积使用浅色中性背景和白色信息面，不滥用高饱和色、渐变或装饰。
- 精密：通过细网格、细分隔线、等宽数字、紧凑标签表达工程系统的秩序感。
- 可信：状态与风险必须明确，颜色含义稳定，交互反馈直接而可验证。
- 实时：薄荷青信号线、状态点和少量脉冲表达“系统正在运行”，但不能让页面持续躁动。

### 1.3 视觉命题与标志性元素

本产品的视觉命题是：**在安静的浅色工作面上，用深色结构锚点组织信息，并以薄荷青标出实时信号。**

最具识别度的元素是“实时信号语法”：

- 32px 浅色技术网格；
- 深海军蓝的导航、标题图标或关键状态面；
- 一条短薄荷青信号线，或一个带柔和光晕的状态点；
- 英文技术眉题与等宽数据作为辅助标记。

同一屏只应有一个主要视觉锚点。大胆设计应集中在页头、关键监控面或场景主画布之一，其余区域保持安静。

## 2. 规范优先级与适用范围

出现冲突时，按以下顺序执行：

1. 本文的设计原则与组件使用规则；
2. `frontend/src/global.css` 中的 `--app-*` 设计令牌；
3. `frontend/src/theme/index.ts` 中的 Ant Design 主题配置；
4. `frontend/src/components/common/` 中的公共组件；
5. 页面内已有样式，仅作为该场景的参考，不能自动视为全局规范。

以下属于受控的独立场景，不可直接反推普通后台页面的样式：

- `/login`：品牌入口页；
- `/alert-wall`：远距离观看的全屏告警大屏；
- `/workflows/editor/:id`：全屏工作流编辑器；
- 算法编排节点：节点类型需要额外的多色语义。

旧样式中仍存在 Ant Design 默认蓝 `#1890ff`、默认红、零散硬编码颜色和重复规则。它们是渐进迁移对象，不是新增代码的依据。

## 3. 设计前的必要定义

开始设计新页面前，先用不超过 10 行写清以下内容，可记录在需求、PR 描述或开发笔记中：

1. **对象**：这个页面在管理或观察什么真实对象；
2. **用户**：主要由运维、算法工程师还是管理员使用；
3. **单一任务**：用户来到页面最需要完成的一件事；
4. **信息优先级**：首屏最先看什么，其次看什么；
5. **颜色**：从既有令牌中选 4–6 个本页需要的颜色；
6. **字体角色**：标题、正文、数据分别如何呈现；
7. **布局**：用一句话和简单线框说明页面结构；
8. **标志性表达**：本页唯一需要强化的状态、流程或画布是什么；
9. **移动端策略**：折行、堆叠、横向滚动或隐藏哪些辅助信息；
10. **自检**：移除一个与任务无关的装饰或动画。

不要为了“有设计感”给所有页面添加英雄区、巨大数字、渐变卡片或编号步骤。结构元素必须编码真实信息：只有确有顺序时才使用步骤编号，只有实时数据才使用 LIVE 和脉冲。

## 4. 基础设计令牌

### 4.1 核心色板

新增样式必须优先使用 `--app-*` 变量，不要复制十六进制值。

| 角色 | CSS 变量 | 当前值 | 用途 |
| --- | --- | --- | --- |
| 页面画布 | `--app-bg` | `#edf1f3` | 全局浅灰背景 |
| 次级画布 | `--app-bg-soft` | `#f4f7f8` | 分区背景、弱层级 |
| 半透明表面 | `--app-surface` | `rgba(255,255,255,.94)` | 需要透出背景纹理的卡片 |
| 实心表面 | `--app-surface-strong` | `#ffffff` | 表格、表单、弹窗主体 |
| 静默表面 | `--app-surface-muted` | `#f4f7f8` | 表头、内嵌块、禁用区 |
| 主文字 | `--app-text` | `#14202b` | 标题、关键值、主要正文 |
| 次文字 | `--app-text-secondary` | `#465866` | 标签、说明、次级正文 |
| 弱文字 | `--app-text-muted` | `#687985` | 辅助信息、时间、占位说明 |
| 边界 | `--app-line` | `#d4dde2` | 明确边框、输入框 |
| 弱边界 | `--app-line-soft` | `#e2e8eb` | 卡片、表格行、内部分隔 |
| 主操作 | `--app-accent` | `#14202b` | 默认主按钮、选中页码 |
| 深色结构面 | `--app-night` | `#0b1722` | 顶栏、图标底、品牌面 |
| 深色结构面二级 | `--app-night-soft` | `#102433` | 深色渐变起点、内嵌面 |
| 实时信号 | `--app-signal` | `#46bfa7` | 选中线、实时曲线、信号标记 |
| 亮信号 | `--app-signal-bright` | `#62d4bb` | 深色面上的图标和状态线 |

### 4.2 语义色

| 语义 | 主色 | 浅底 | 使用条件 |
| --- | --- | --- | --- |
| 信息 | `--app-info` | `--app-info-soft` | 查看、详情、帮助、处理中 |
| 成功 | `--app-success` | `--app-success-soft` | 正常、运行、启用、已完成 |
| 警告 | `--app-warning` | `--app-warning-soft` | 注意、排空、可能影响运行 |
| 危险 | `--app-danger` | `--app-danger-soft` | 异常、告警、删除、不可逆操作 |

语义色规则：

- 颜色首先传递状态，不承担页面装饰；
- 同一状态在按钮、徽标、图表和提示中使用同一语义色；
- 浅底配深色文字，实心色只用于高优先级操作或小面积状态；
- 不能只靠颜色表达状态，必须同时提供文字、图标或形状；
- 薄荷青表示实时信号或当前路径，不等同于所有“成功”状态；成功状态仍使用 `--app-success`。

### 4.3 渐变与背景

- 普通内容区使用纯色或轻透明白色，不为每张卡片添加渐变。
- 深色渐变仅用于导航、登录品牌区、关键监控面或工作流编辑器顶栏。
- 页面背景沿用 32px 网格，线条透明度保持很低，不能影响文字阅读。
- 允许在深色主视觉中加入一个低对比径向光晕或圆环，但不能同时堆叠多种装饰。
- 图片上的渐变只用于保证文字可读，不能改变告警证据本身的可辨识度。

## 5. 字体与信息层级

### 5.1 字体角色

- 中文和正文：`PingFang SC`, `Hiragino Sans GB`, `Microsoft YaHei`, `Segoe UI`, sans-serif。
- 数据、版本、ID、代码、时间：`SFMono-Regular`, `Consolas`, `Liberation Mono`, monospace。
- 告警大屏可使用 `Noto Sans SC`；仅数字大屏可使用 `Orbitron`。不得将这两种字体扩散到普通后台页面。
- 不引入新的远程字体，除非产品明确需要新的品牌方向并确认了加载与降级策略。

### 5.2 推荐字号层级

| 层级 | 字号 | 字重 | 说明 |
| --- | --- | --- | --- |
| 品牌主标题 | `38–56px` | `600–650` | 仅登录页或同级品牌入口 |
| 页面标题 | `clamp(24px, 2vw, 30px)` | `680` | `PageHeader` 标准 |
| 区块标题 | `20–22px` | `650–700` | 主要监控模块、工作台区域 |
| 卡片标题 | `15–17px` | `600–650` | 卡片与弹窗标题 |
| 正文 | `13–14px` | `400–500` | 表单说明、内容文本 |
| 表格正文 | `12–14px` | `400–600` | 名称可加粗，辅助信息更小 |
| 辅助标签 | `10–12px` | `500–700` | 状态、时间、元数据 |
| 技术眉题 | `9–11px` | `700` | 大写英文、等宽或系统字体 |
| 关键数据 | `17–32px` | `650–700` | 使用等宽字体和等宽数字 |

排版规则：

- 页面标题使用略紧字距 `-0.03em` 至 `-0.045em`，正文不压缩字距。
- 技术眉题使用 `0.12em–0.16em` 字距和大写英文，内容必须真实描述模块，不写营销口号。
- 数值使用 `font-variant-numeric: tabular-nums`，单位与值分层显示。
- 正文行高约 `1.55`，长说明可到 `1.65–1.85`。
- 中文界面使用句式大小写；英文技术标签可全大写。
- 不把低于 10px 的文字用于必须阅读或操作的信息。

## 6. 间距、圆角、边框与阴影

### 6.1 间距

基础间距采用 4px 网格：`4 / 8 / 12 / 16 / 24px`。推荐组合：

- 图标与文字：`6–8px`；
- 同组控件：`8–12px`；
- 卡片内部：`16–24px`；
- 页面区块之间：`20–24px`；
- 首屏外边距由应用壳统一控制，不在每个页面重复添加。

只有品牌页、大屏和复杂工作台可使用 `28–86px` 的场景化间距。避免随意出现 5px、17px、23px 等无法解释的新值。

### 6.2 圆角

| 对象 | 推荐圆角 |
| --- | --- |
| 小标签、局部节点 | `4–8px` |
| 输入框、按钮、页码 | `--app-radius-control: 10px` |
| 小卡片、警告、移动端弹窗 | `--app-radius-s: 12px` |
| 标准卡片、表格、弹窗、顶栏 | `--app-radius-m: 18px`，紧凑区可用 `14–16px` |
| 品牌级大容器 | `20–26px` |
| 状态徽标、进度条、圆点 | `999px` 或 `50%` |

圆角表达层级，而不是装饰。内层圆角应小于外层圆角。

### 6.3 边框与阴影

- 卡片默认使用 `1px solid var(--app-line-soft)`。
- 输入和需要明确边界的控件使用 `var(--app-line)`。
- 常规卡片使用 `--app-shadow-sm`；浮层使用 `--app-shadow-md` 或 `--app-shadow-lg`。
- 不同时使用粗边框、强阴影和高饱和背景。
- Hover 主要通过边框、浅底或小幅阴影变化表达，不让普通卡片大幅上浮。

## 7. 页面布局系统

### 7.1 应用壳

- 普通页面使用 `.app-shell`、`.app-shell__main` 和 `.app-shell__content`。
- 内容最大宽度为 `1760px`，在超宽屏居中。
- 桌面端主内容边距：纵向 `24px`，横向 `clamp(18px, 2.4vw, 36px)`。
- 移动端主内容边距：约 `14–16px`，底部保留 `28–36px`。
- 页面根元素必须 `min-width: 0`，防止 Grid、Flex 和表格撑破容器。

### 7.2 顶部导航

- 顶栏固定在视口顶部，深色半透明，圆角 `18px`，承担全局结构锚点。
- 桌面端品牌在左、导航居中、工具与用户在右。
- `1240px` 以下导航换到第二行并可横向滚动。
- `768px` 以下隐藏品牌副标题、公司名和用户元信息，只保留核心识别与操作。
- 当前项使用浅薄荷底和底部信号线，不使用整块高亮主色。

### 7.3 标准管理页骨架

```text
┌─ PageHeader：图标 + 眉题 + 标题 + 说明 ───── 数量 + 页面主操作 ┐
├─ 可选 AppToolbar：筛选 / 搜索 ───────────── 汇总 / 批量操作 ┤
├─ 主要内容：表格、卡片网格、工作台或状态面板                  ┤
└─ 分页 / 空状态 / 加载或错误反馈                              ┘
```

- 普通管理页必须优先使用 `PageHeader`，不要各自实现新的大标题区。
- 标题说明写清用户能做什么；右侧只放计数和本页最高频操作。
- 页面只能有一个默认实心主按钮。次级动作使用 outline 或 text。
- 筛选较多时使用 `AppToolbar`；不要把筛选器零散漂浮在页面中。
- 数据密集页面优先使用表格；需要比较状态与元数据时使用卡片网格。

### 7.4 响应式策略

默认断点：

- `>1240px`：完整桌面导航；
- `769–1240px`：导航折为第二行，复杂 Grid 减列；
- `577–768px`：页面标题、工具栏和操作区纵向堆叠；
- `<=576px`：单列内容、紧凑内边距、隐藏非关键辅助信息。

响应式不是等比缩小：

- Flex/Grid 子项必须设置 `min-width: 0`；
- 表格保留语义完整性，必要时横向滚动，不强行挤压所有列；
- 操作按钮可整行铺满，仍保持主次顺序；
- 工具栏从水平排列改为纵向；
- 次要统计和说明可以隐藏，但核心状态、对象名称和主操作必须保留；
- 复杂编辑器在窄屏压缩侧栏；如果任务无法可靠完成，应明确给出桌面端使用提示，而不是制造不可用的小画布。

## 8. 公共组件规范

新增页面先复用 `frontend/src/components/common/`，确认现有组件无法满足后再扩展公共组件。

### 8.1 页面标题 `PageHeader`

- 必填：有含义的图标、中文标题。
- 推荐：英文技术眉题、单句副标题、对象计数。
- `extra` 中放页面级操作，不放行级操作。
- 标准标题下方有细分隔线和 76px 薄荷信号线，这是普通页面的统一签名。

### 8.2 按钮 `AppButton`

按钮由两个维度组成：

- `variant="solid | outline | text"`：决定操作优先级；
- `tone="neutral | info | success | warning | danger"`：决定语义。

使用原则：

- 页面主要创建、保存：`solid + neutral`；
- 查看、预览、详情：`outline/text + info`；
- 启动、确认正常状态：`success`；
- 风险提醒但可继续：`warning`；
- 删除、清空、终止：`danger`；
- 只有图标时设置 `iconOnly`，并提供 `aria-label` 与 Tooltip；
- 按钮文本使用明确动词，如“保存配置”“上传模型”“返回告警记录”，不写“确定”“提交”这类脱离上下文的词。

控件标准高度为 `40px`，小号 `32px`，大号 `44px`；登录主操作可为 `50–52px`。

### 8.3 工具栏 `AppToolbar`

- 左侧为筛选和搜索，中部可显示结果摘要，右侧为清空或批量操作。
- 同一行控件间距 `8–16px`，移动端纵向排列。
- 搜索应说明搜索对象，筛选值应可清空，重置操作只在存在筛选条件时强调。

### 8.4 表格

- 表格包裹层使用白色实心面、弱边框、`14–18px` 圆角和轻阴影。
- 表头为静默浅灰底，`11–12px / 600`，可使用轻微大写和字距。
- 单元格通常使用 `16px` 内边距；高密度弹窗内可适当收紧。
- 名称列优先显示对象名称，再显示编码、版本或来源。
- 长文本必须省略并提供 Tooltip 或详情入口。
- 行 Hover 只使用极浅背景变化。
- 行操作使用 `AppButton` 的 text/outline 变体；危险操作始终放在末尾。

### 8.5 卡片与监控面板

- 标准卡片：白色或 `rgba(255,255,255,.88–.94)`、弱边框、轻阴影。
- 卡片内先给标签，再给关键值和说明；数据用等宽字体。
- 状态卡片可使用 5px 进度条，正常、警告、危险按语义变色。
- 深色卡片只用于硬件状态、节点身份、实时监控等需要明显结构分区的内容。
- 一组同级卡片高度、内边距和标题位置必须一致。

### 8.6 状态 `StatusBadge` 与 `SwitchBadge`

- 离散状态优先使用 `StatusBadge`，形态为文字胶囊 + 小状态点。
- 可直接切换的启用状态使用 `SwitchBadge`；只读状态不要伪装成开关。
- 动画只用于 `STARTING / RUNNING / DRAINING / ERROR` 等实时状态。
- “停止”用静态方点以区别运行中的圆点。
- 状态文字使用业务语言，如“检测中”“等待中”“异常”，不只显示内部枚举。

### 8.7 弹窗 `AppModal`

根据任务选择类型和尺寸：

| 类型 | 用途 | 常用尺寸 |
| --- | --- | --- |
| `form` | 新建、编辑 | `sm / md / lg` |
| `detail` | 查看对象详情 | `md / lg` |
| `inspect` | 检测、验证、诊断 | `lg / xl` |
| `media` | 图片或视频证据 | `lg / xl` |
| `fullscreen` | 复杂编辑与沉浸式任务 | `full` |

- 标题区使用 3px 语义信号条、标题和可选说明。
- 表单弹窗默认不可点击遮罩误关；查看型或媒体型可以关闭。
- 正文默认可滚动，头部和底部保持稳定。
- 底部按钮从左到右遵循“取消/返回 → 次级 → 主操作”，危险确认清楚说明对象与后果。
- 不在页面 CSS 中重新定义公共弹窗壳。

### 8.8 空、加载、错误与确认

- 空状态使用 `AppEmptyState`，包含“发生了什么”和“下一步做什么”。
- 加载中保留布局稳定，避免内容跳动；长任务显示阶段或进度。
- 错误提示说明具体失败对象和恢复方式，例如“无法读取系统指标，请检查服务权限或稍后重试”。
- 删除等破坏性操作使用 `useAppConfirm`，明确对象名称、影响和不可逆性。
- 操作名称与反馈保持一致：“保存配置”完成后提示“配置保存成功”。

## 9. 页面类型

### 9.1 仪表盘

- 目标是回答“现在是否正常”，不是塞入所有管理入口。
- 首屏顺序为资产概览 → 系统指标 → 告警趋势与通道分布。
- 统计卡保持浅色安静，图标底表达分类；危险红只给真实告警。
- 图表使用弱网格、薄荷主曲线、等宽坐标和明确时间范围。
- 实时刷新要显示更新时间或陈旧状态，且不能频繁闪烁整块内容。

### 9.2 列表与资产管理页

- 使用标准 `PageHeader + AppToolbar + Table/Card grid`。
- 计数显示当前结果还是全部结果必须在文案上说清楚。
- 页面级创建放页头，行级查看、编辑、删除留在行内。
- 自动刷新不能覆盖用户正在编辑或选择的状态。

### 9.3 设置页与复杂表单

- 按用户目标分 Tab，不按后端模块名机械分组。
- 每张设置卡只负责一类配置，标题下给影响范围说明。
- 相关字段使用 2 列 Grid，`<=560–640px` 降为单列。
- 运行环境、节点身份、资源状态可使用一个深色信息条作为视觉锚点。
- 保存按钮保持固定命名；未保存、保存中、已保存和失败必须有反馈。

### 9.4 向导与流程

- 仅当步骤必须按顺序完成时使用步骤条。
- 当前、已完成、不可用状态同时用文字、颜色和形状区分。
- 每一步只提出当前需要的决策；高级配置折叠或延后。
- 返回上一步不丢失输入，最终提交前给出摘要和影响范围。

### 9.5 工作流编辑器

- 这是全屏工具，不使用普通应用壳。
- 基本结构为：深色工具栏 + 左侧组件库 + 中间无限画布 + 右侧属性/测试面板。
- 节点颜色表达类型：视频、算法、条件、ROI、函数、告警等可保留各自语义色。
- 连线、端口和选中态必须清晰；装饰动画不能妨碍拖拽、缩放和连线。
- 新增样式应逐步以 `--app-*` 替代旧的 Ant 默认色，同时保留节点类型之间的可辨识度。
- 编辑器内通用按钮、弹窗、空状态仍复用公共组件。

### 9.6 登录页

- 登录页承担品牌介绍与安全访问两个任务，桌面端为深色品牌面 + 白色表单面的双栏结构。
- 品牌面的“视频扫描框”是场景化标志元素，不复制到普通表单页。
- `<=980px` 隐藏品牌面，保留紧凑品牌标记；`<=560px` 使用无卡片的全屏表单。
- 登录表单只有一个主操作，不增加与登录无关的入口。

### 9.7 告警大屏

- 告警大屏服务于远距离、持续观看，允许使用高对比深色主题、发光数字和更强实时动效。
- 支持蓝、绿、紫、琥珀、青主题，但每个主题内部的状态语义要一致。
- 重要内容按“主告警证据 → 当前统计 → 最近告警列表”组织。
- 扫描线、粒子和脉冲必须尊重 `prefers-reduced-motion`，且不能遮挡证据或操作。
- 这套霓虹与发光语言不得带入普通后台管理页。

## 10. 图标、图片与数据可视化

### 10.1 图标

- 默认使用 `@ant-design/icons`，保持同一套线性图标语言。
- 图标用于识别对象或操作，不重复装饰已经清楚的标题。
- 普通按钮图标约 `13–16px`，标题图标约 `19–22px`，品牌标志可到 `28px`。
- 图标底通常为 28–46px 的圆角方形；同一组尺寸必须一致。

### 10.2 图片与视频

- 告警截图、检测帧和实时视频是证据内容，优先保证完整比例和可辨识度。
- 缩略图可裁切，详情图使用 `object-fit: contain`。
- 加载失败提供业务化占位，不显示浏览器破图。
- 媒体弹窗应尽量提供时间、通道、工作流和告警类型等上下文。

### 10.3 图表

- 默认使用薄荷青主序列，语义色用于异常阈值和状态区间。
- 坐标、时间和数值使用等宽字体；网格线使用 `--app-line-soft`。
- 图表必须有标题、时间范围、单位、空状态和错误状态。
- 动画只用于刷新过渡或实时点，不做无意义的循环演示。
- 颜色序列之外还要用标签、形状或线型帮助区分。

## 11. 交互与动效

- 快速反馈：`--app-motion-fast: 160ms`，用于按钮、边框、颜色与小位移。
- 基础过渡：`--app-motion-base: 220ms`，用于卡片、面板和状态变化。
- 数据趋势可使用约 `400ms` 的宽度/路径过渡。
- 单次进入动画可使用淡入或小幅 `translateY`，位移控制在 `8–20px`。
- Hover 位移通常不超过 `1–2px`；普通按钮禁止弹跳或缩放。
- 脉冲仅表示真实的运行、刷新或告警状态。
- 全局和独立场景都必须实现 `prefers-reduced-motion: reduce`。
- 键盘焦点统一使用可见的薄荷青 2px 外框，不能仅靠 Hover。

## 12. 界面文案

- 从用户可识别的对象和动作出发，不暴露不必要的内部实现名。
- 使用主动语态和具体动词：“创建工作流”“保存配置”“重新检测”。
- 一个术语在标题、按钮、提示和成功消息中保持一致。
- 标题短而明确；副标题说明范围或结果，不写空泛宣传语。
- 错误不说笼统的“操作失败”，应说明失败对象和恢复方法。
- 空状态是行动入口，不是情绪表达。
- 危险操作写明对象、依赖影响与是否可恢复。
- 标点使用中文全角；技术标识、路径和枚举保持原格式。

## 13. 可访问性底线

- 所有交互元素可通过键盘访问，并有清晰 `:focus-visible`。
- 图标按钮必须有 `aria-label`；纯装饰元素设置 `aria-hidden="true"`。
- 状态不能只依赖颜色；图表不能只依赖颜色区分序列。
- 正文与背景对比度至少 4.5:1，大号文字至少 3:1。
- 点击目标不小于 40×40px；紧凑表格中的次要图标按钮最低 32×32px。
- 表单控件有可见标签，错误信息与字段关联。
- 弹窗打开后管理焦点，关闭后返回触发元素。
- 动态刷新避免抢夺焦点，并通过合适的 `aria-live` 提示真正重要的变化。
- 视频、图片和图表提供替代文字或邻近文本摘要。

## 14. CSS 与 React 实现约束

### 14.1 必须执行

- 使用 `appTheme` 配置 Ant Design，使用 `--app-*` 变量写页面 CSS。
- 公共交互优先使用 `AppButton`、`AppModal`、`AppToolbar`、`AppEmptyState`、`StatusBadge`、`SwitchBadge`、`PageHeader` 与 `useAppConfirm`。
- 页面样式以页面根类作用域开头，复杂组件采用 BEM 风格命名。
- 组件处理 `min-width: 0`、文本溢出、加载、空、错误、禁用和移动端状态。
- 样式文件与组件就近存放；全局规则和令牌只进入 `global.css` 或 `theme/index.ts`。
- 动态数值、几何位置等运行时数据可使用内联样式；稳定视觉值写入 CSS。

### 14.2 禁止新增

- 绕过令牌复制 `#1890ff`、`#1677ff` 等 Ant 默认色作为品牌主色；
- 在普通页面重新实现 PageHeader、按钮、弹窗外壳或状态徽标；
- 无页面根作用域的 `.header`、`.title`、`.content` 等泛化选择器；
- 用大量 `!important` 互相覆盖，公共组件的防遗留覆盖规则除外；
- 每张卡片不同的渐变、强阴影、发光边框或上浮动画；
- 仅为装饰而添加编号、英文标签、粒子、扫描线或持续脉冲；
- 将告警大屏的霓虹风格直接复制到管理后台；
- 仅适配固定桌面宽度或仅靠隐藏内容解决移动端问题。

### 14.3 需要扩展令牌时

只有在现有令牌无法表达跨两个及以上页面都会复用的角色时，才新增全局令牌：

1. 先说明语义角色，不以具体页面或颜色命名；
2. 同步更新 `global.css` 和必要的 `theme/index.ts`；
3. 在本文色板或尺寸章节登记；
4. 替换重复硬编码，而不是新增另一套近似值。

## 15. 开发流程与验收清单

### 15.1 推荐流程

1. 阅读本规范和相邻页面实现；
2. 完成第 3 节的页面定义；
3. 选择页面类型和公共组件；
4. 先完成信息层级与响应式布局，再添加视觉细节；
5. 检查设计是否像本产品，而不是任意后台模板；
6. 删除一个无业务意义的装饰；
7. 在桌面、平板和手机宽度进行实际渲染检查；
8. 用键盘走完主流程，并验证加载、空、错误与长文本。

### 15.2 Definition of Done

页面合并前必须确认：

- [ ] 页面任务、首要信息和主操作清楚；
- [ ] 普通页面使用统一应用壳与 `PageHeader`；
- [ ] 同屏只有一个实心主按钮或能解释的唯一视觉主操作；
- [ ] 颜色来自令牌，状态色语义一致；
- [ ] 标题、正文、数据和辅助信息层级清楚；
- [ ] 复用了合适的公共组件，没有复制公共壳样式；
- [ ] 处理了加载、空、错误、禁用、成功和危险确认；
- [ ] 长名称、长编码、超大数字和无图片数据不会破坏布局；
- [ ] 在 `1440px`、`1024px`、`768px`、`390px` 宽度下可用；
- [ ] 表格与复杂画布有明确的窄屏策略；
- [ ] 键盘焦点、标签、ARIA、对比度与点击尺寸合格；
- [ ] 动效克制且支持 reduced motion；
- [ ] 文案具体、一致，反馈与操作命名对应；
- [ ] 已通过实际页面截图检查，而不只阅读代码；
- [ ] 没有把受控独立场景的视觉语言误用到普通页面。

## 16. 当前实现索引

后续开发优先参考以下文件：

- 全局令牌与基础覆盖：`frontend/src/global.css`
- Ant Design 主题：`frontend/src/theme/index.ts`
- 应用壳：`frontend/src/layouts/index.tsx`
- 顶部导航：`frontend/src/components/Header.tsx`、`Header.css`
- 公共组件：`frontend/src/components/common/`
- 标准管理页：`frontend/src/pages/video-sources/`、`algorithms/`、`scripts/`
- 仪表盘：`frontend/src/pages/dashboard/`
- 复杂设置：`frontend/src/pages/system-settings/`
- 工作台布局：`frontend/src/pages/face-galleries/`
- 全屏编辑器：`frontend/src/pages/workflows/editor/`
- 品牌登录页：`frontend/src/pages/login/`
- 告警大屏：`frontend/src/pages/alert-wall/`

当现有页面与本文不一致时，新增功能按本文执行；触及旧组件时，在不扩大需求范围的前提下顺手向本文规范收敛。
