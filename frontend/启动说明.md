# 启动步骤

## 1. 安装依赖
```bash
cd frontend
npm install
```

## 2. 启动后端（另一个终端）
```bash
cd ..
pip install flask-cors
python -m app.web.webapp
```

## 3. 启动前端
```bash
npm run dev
```

访问: http://localhost:8000

## 生产构建
```bash
npm run build
# 构建产物在 dist/ 目录
```

